# Zukunft

GitHub Projects v2 のスケジュールを Gantt で編集するデスクトップアプリ。
Tauri (Rust) + Next.js のモノレポ。リポジトリは `kakanzler/Zukunft`。

- `apps/desktop` — Tauri 本体（`src-tauri` が Rust、`next` が UI）
- `apps/web` — 読み取り専用の Web ビュー
- `packages` — 2 つのアプリで共有するドメイン / GitHub アクセス層
- `specifications` — 企画書。コード中のコメントが「企画書 §N」と参照している正本

## 未着手のものは Issue に残す

作業の中で見つけたが、**その作業では直さなかったもの**は、報告して終わりにせず
このリポジトリの Issue として登録する。会話が閉じれば消える情報にしないため。

登録するもの:

- 直したバグの周辺で見つかった、別の未修正のバグ
- スコープ外として意図的に見送った改善・リファクタ
- サブエージェントが「スコープ外で気づいた点」として報告してきたもの
- 暫定対応で塞いだだけの箇所、コードに残した TODO / FIXME
- 再現条件が作れず検証を見送った項目

登録しないもの:

- その作業の中で直したもの
- 既に Issue があるもの（重複を作らない）
- そのセッション限りのスクラッチや一時ファイルの話

**手順**: `register-todo-as-issue` スキルを使う。`gh issue create` を手で組み立てない。
起票は外部に出る操作なので、下書きを提示して承認を得てから実行する。

作業の最終報告には、登録した Issue の番号と URL を必ず含める。見送った項目を
文章で述べるだけで終わらせない。

{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Zukunft のメインウィンドウに与える権限。GitHub 操作は Rust 側の command で行うため、フロントに与える権限は最小限にとどめる（企画書 §17）。",
  "windows": ["main"],
  "permissions": ["core:default", "opener:allow-open-url"]
}
